<!-- @generated by src/parity_report.rs — do not edit by hand.
Regenerate: NEVI_UPDATE_PARITY=1 cargo test parity_report -->

# Vim/Neovim Parity Scoreboard

Nevi aims for full Vim/Neovim keybind compatibility — and backs the claim with
tests instead of adjectives. This page is generated from the same inventories
the test suite enforces, so it cannot drift from what is actually verified.

## Summary

- **329 keybinds implemented** ([KEYBINDINGS.md](KEYBINDINGS.md)), **39 planned** ([KEYBINDS_ROADMAP.md](KEYBINDS_ROADMAP.md))
- **73 keybinds in the coverage inventory**, each mapped to the automated test that protects it:
  - 71 verified against real Neovim (v0.11.3) by the Vim oracle
  - 1 protected by focused Nevi regression tests
  - 1 covered as default-keymap plumbing with dedicated tests
- **221 oracle cases**: motions (107), editing (56), insert-entry (11), open-line (18), replace (27), undo-redo (2)
- **0 tracked coverage gaps**

## How the Vim oracle works

Each oracle case is an initial buffer plus a literal key sequence. The keys are
replayed in Nevi and in `nvim --headless`, and the resulting buffer content,
cursor position, viewport top, and mode are compared. Cases run
deterministically in the normal test suite; CI additionally replays them
against a pinned real Neovim (v0.11.3) on macOS and Linux
(`NEVI_VIM_ORACLE=1`). A failure report includes both editors' snapshots.

The coverage inventory (`src/keybind_coverage.rs`) is itself enforced by tests:
entries must be unique and complete, oracle references must name real cases,
and any gap must carry an explicit written reason — a keybind cannot silently
claim protection.

## Verified against real Neovim

| Keybind | Behavior | Oracle case |
|---------|----------|-----------|
| `h` | Move cursor left | `move left` |
| `j` | Move cursor down | `move down` |
| `k` | Move cursor up | `move up` |
| `l` | Move cursor right | `move right` |
| `w` | Move to start of next word | `word forward` |
| `b` | Move to start of previous word | `word backward` |
| `e` | Move to end of word | `word end` |
| `0` | Move to start of line | `line start` |
| `^` | Move to first non-blank character | `first nonblank` |
| `$` | Move to end of line | `line end` |
| `g_` | Move to last non-blank character | `last non blank` |
| `\|` | Move to column [count] | `to column` |
| `gM` | Move to middle of the line's text | `middle of line` |
| `]]` | Move to next section start | `section forward` |
| `[[` | Move to previous section start | `section backward` |
| `][` | Move to next section end | `section end forward` |
| `[]` | Move to previous section end | `section end backward` |
| `[{` | Move to previous unmatched { | `unmatched open brace` |
| `]}` | Move to next unmatched } | `unmatched close brace` |
| `[(` | Move to previous unmatched ( | `unmatched open paren skips nested pair` |
| `])` | Move to next unmatched ) | `unmatched close paren skips nested pair` |
| `gm` | Move to middle of the screen line | `gm on short line` |
| `go` | Go to [count] byte of the buffer | `go to byte` |
| `<CR>` | Move to first non-blank of next line | `enter next line first nonblank` |
| `gg` | Move to start of file | `file top` |
| `G` | Move to end of file | `file bottom` |
| `x` | Delete character under cursor | `delete first char on second line` |
| `dd` | Delete current line | `delete current line` |
| `D` | Delete to end of line | `delete to line end` |
| `i` | Insert before cursor | `insert before cursor` |
| `I` | Insert at first non-blank | `insert at first nonblank` |
| `a` | Append after cursor | `append after cursor` |
| `A` | Append at end of line | `append punctuation at line end` |
| `o` | Open line below | `open line below` |
| `O` | Open line above | `open line above` |
| `dw` | Delete word with motion | `delete word` |
| `ciw` | Change inner word | `change inner word` |
| `cc` | Change current line | `change current line` |
| `C` | Change to end of line | `change to line end` |
| `yy` | Yank current line | `yank current line` |
| `Y` | Yank through line end | `yank to line end` |
| `p` | Paste after cursor | `paste after linewise yank` |
| `P` | Paste before cursor | `paste before linewise yank` |
| `de` | Delete through word end | `delete to word end` |
| `db` | Delete to previous word start | `delete to previous word start` |
| `d$` | Delete through line end | `delete with line-end motion` |
| `caw` | Change around word | `change around word` |
| `u` | Undo latest change | `undo insert` |
| `<C-r>` | Redo latest undone change | `redo insert` |
| `W` | Move to start of next WORD | `big word forward` |
| `B` | Move to start of previous WORD | `big word backward` |
| `E` | Move to end of WORD | `big word end` |
| `ge` | Move to end of previous word | `previous word end` |
| `gE` | Move to end of previous WORD | `previous big word end` |
| `%` | Jump to matching bracket | `matching bracket` |
| `H` | Move to top of visible screen | `screen top` |
| `M` | Move to middle of visible screen | `screen middle` |
| `L` | Move to bottom of visible screen | `screen bottom` |
| `f{char}` | Find character forward on current line | `find char forward` |
| `F{char}` | Find character backward on current line | `find char backward` |
| `t{char}` | Move before character forward on current line | `till char forward` |
| `T{char}` | Move after character backward on current line | `till char backward` |
| `;` | Repeat latest find-character search | `repeat find char` |
| `,` | Repeat latest find-character search in reverse | `reverse repeat find char` |
| `<C-f>` | Scroll page down | `page down` |
| `<C-b>` | Scroll page up | `page up` |
| `<C-d>` | Scroll half page down | `half page down` |
| `<C-u>` | Scroll half page up | `half page up` |
| `zz` | Center cursor line | `center cursor line` |
| `zt` | Move cursor line to top | `cursor line to top` |
| `zb` | Move cursor line to bottom | `cursor line to bottom` |

## Protected by Nevi regression tests

Nevi-owned behavior with no Vim equivalent to compare against.

| Keybind | Behavior | Test |
|---------|----------|----|
| `<leader>j` | Start labeled jump navigation | `labeled_jump_jumps_to_selected_visible_match` |

## Default-keymap plumbing

| Keybind | Behavior | Test |
|---------|----------|----|
| `<leader>fk` | Open searchable keymap picker | `default_leader_includes_keymaps_picker` |

## Tracked gaps

None — every inventoried keybind is currently protected by a test.
